// Student.cpp is small because Student is an alias to Person.
// We keep this file to match your requested structure; it doesn't need to contain code,
// but we provide a small helper function example (optional).

#include "Student.h"

// No definitions required because Student is an alias of Person.
// This file intentionally left minimal to satisfy project layout.
